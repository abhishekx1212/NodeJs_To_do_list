<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-6">
                <h1>To do list</h1>
                <form action="/insertData" method="post">
                    <input type="text" name="taskName" id="nameInput" class="form-control border-danger mb-2"
                        placeholder="Enter task name">
                    <input type="submit" name="" id="" value="ADD TASK" class="bg-primary text-light border-0 rounded-2 px-2">
                    <div class="form-content pt-5">
                        <div class="row gy-3">
                            <% givenTask.map((val)=>{ %>
                                <div class="col-12 d-flex border border-secondary align-items-baseline justify-content-between py-1 rounded-2">
                                    <div class="info-Block d-flex">
                                        <h3 class="d-none">
                                            <%= val.id %>
                                        </h3>
                                        <h3>
                                            <%= val.taskName %>
                                        </h3>
                                    </div>
                                    <div class="a-tags">
                                        <a href="/editTask?id=<%= val.id %>" class="fs-4 me-2 text-light bg-warning px-2 rounded-2" style="text-decoration: none;">EDIT</a>
                                        <a href="/deleteTask?id=<%= val.id %>" class="fs-4 text-light bg-danger px-2 rounded-2" style="text-decoration: none;">DELETE</a>
                                    </div>
                                </div>
                                <% }) %>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>

</html>